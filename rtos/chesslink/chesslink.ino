// arduino IDE entry point -- all real code lives in the .cpp files
// IDE compiles everything in this folder, so just include the shared header
#include "chesslink.h"

// setup() and loop() are defined in main.cpp
